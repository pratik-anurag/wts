[package]
name = "wts-git"
version = "0.1.0"
edition = "2024"
license = "Proprietary"
rust-version = "1.85"

[dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
serde_json.workspace = true
tempfile = "3"
