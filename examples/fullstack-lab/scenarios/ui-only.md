# UI-101 — Clarify the checkout action

Scope: `storefront-ui`

Change the checkout button copy from “Submit order” to “Place order”. Do not
change the API repository or the injected `wts_ui_101.test.js` acceptance test.

Verification: `npm test`
