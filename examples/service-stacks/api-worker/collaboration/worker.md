# Worker workstream

Update `processJob` to execute the normalized job operation.

- `double` returns `input * 2`.
- `square` returns `input * input`.
- Throw for an unknown operation.
- Preserve the result shape.
- Edit only `src/processor.mjs`.
- Run `npm test --silent`.

The API agent is implementing the matching validation behavior in parallel.
