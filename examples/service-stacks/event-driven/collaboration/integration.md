# Integration verification

After all three agents finish, run each package test suite. Start the isolated
stack and run `node collaboration/smoke.mjs`. The proof must observe the
currency only through the read API after asynchronous projection. Reading
event files directly is not an integration proof.
